# 🌉 Vocelio.ai API Gateway Requirements
# Enterprise-grade API Gateway with advanced routing and middleware

# Core Framework (Standardized)
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0
pydantic-settings==2.1.0
python-multipart==0.0.6

# Database & Caching (Standardized)
asyncpg==0.29.0
redis[hiredis]==5.0.1

# HTTP & Networking (Standardized)
httpx[http2]==0.25.2
aiohttp==3.9.1

# Security & Authentication (Standardized)
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
# Relaxed due to build resolution issues on slim image
cryptography>=41.0.0,<42.0.0
PyJWT==2.8.0

# Middleware & Utilities (Standardized)
python-dotenv==1.0.0
structlog==23.2.0
# Removed invalid circuit-breaker package (nonexistent); built-in logic used instead
backoff==2.2.1

# Rate Limiting & Monitoring (Standardized)
slowapi==0.1.9
prometheus-client==0.19.0
sentry-sdk[fastapi]==1.38.0

# Production Server (Standardized)
gunicorn==21.2.0
psutil==5.9.6

# Development & Testing (Standardized)
pytest==7.4.3
pytest-asyncio==0.21.1
