[build]
builder = "NIXPACKS"

[deploy]
startCommand = "python call_center_main.py"
healthcheckPath = "/health"
healthcheckTimeout = 300
watchPaths = ["apps/call-center/**"]

[env]
PORT = "8002"
ENVIRONMENT = "production"
