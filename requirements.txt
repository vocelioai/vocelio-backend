# Vocelio Backend - Root Dependencies
# World-class AI Call Center Platform

# Core Framework (centralized pins via constraints)
-c constraints.txt
fastapi
uvicorn[standard]
gunicorn==21.2.0

# Database & ORM
sqlalchemy
alembic==1.12.1
asyncpg
# Redis client (dramatiq 1.14.x requires redis <5). If upgrading dramatiq, you can raise this to 5.x
redis[hiredis]

# Authentication & Security
python-jose[cryptography]
passlib[bcrypt]
python-multipart
cryptography

# HTTP & API
httpx[http2]
aiohttp
requests==2.31.0

# Data Processing & Validation
pydantic
pydantic-settings==2.1.0
email-validator==2.1.0

# AI & ML - REMOVED problematic langchain conflict
# langchain==0.0.350
# langchain-community==0.0.1
openai==1.3.7
anthropic==0.7.7
elevenlabs==0.2.26

# Telephony & Communication
twilio==8.10.3
plivo==4.55.0

# Audio Processing - REMOVED heavy deps causing build issues
# soundfile==0.12.1  
# librosa==0.10.1
# pydub==0.25.1
numpy

# Background Tasks & Queues - REMOVED dramatiq due to redis conflict  
# celery==5.3.4
# dramatiq==1.14.2

# Monitoring & Logging
sentry-sdk[fastapi]==1.38.0
structlog
prometheus-client

# File Storage & Cloud
boto3==1.34.0
google-cloud-storage==2.10.0
azure-storage-blob==12.19.0

# Payment Processing
stripe==7.8.0
paypal-checkout-serversdk==1.0.1

# Testing
pytest
pytest-asyncio
pytest-cov
factory-boy==3.3.0

# Development Tools
black
isort
flake8
mypy
pre-commit

# Environment & Configuration
python-dotenv

# Date & Time
arrow==1.3.0
pendulum==2.1.2

# System Monitoring & Performance
psutil==5.9.6

# Additional Redis Support
## (redis & httpx extras consolidated above)

# JSON Web Tokens (now in constraints.txt)
PyJWT

# Supabase Client
supabase==2.0.2

# Utilities
click
rich==13.7.0
typer==0.9.0

# Enterprise Features
backoff==2.2.1

# NOTE:
# - Removed duplicate entries (redis, httpx, pydantic-settings)
# - Consolidated extras: redis[hiredis], httpx[http2]
# - Removed 'circuit-breaker' (custom circuit breaker logic implemented internally)
